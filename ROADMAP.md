# CLA Spec Kit — Roadmap

This is the development roadmap for the spec-kit tool itself — not for apps generated by it.

Status legend: ✅ Done · 🔄 In progress · ⬜ Not started

---

## Phase 1 — Foundation

The core scaffolding workflow, working end-to-end with one fixed stack and a complete example app.

| # | Item | Status |
|---|---|---|
| 1.1 | `/design` Copilot prompt — reads `ideas/<app>/ideas.md` + images, writes 16 requirement docs | ✅ |
| 1.2 | `/plan` Copilot prompt — reads requirements, writes `plans/<app>/PLAN.md` | ✅ |
| 1.3 | `/create` Copilot prompt — reads plan + specs, scaffolds three repos from templates | ✅ |
| 1.4 | `/validate` Copilot prompt — audits a generated scaffold against specs, produces scored report | ✅ |
| 1.5 | Node.js + Fastify backend template (`templates/framework-nodejs-starter-kit/`) | ✅ |
| 1.6 | React + Vite + MUI frontend template (`templates/framework-react-starter-kit/`) | ✅ |
| 1.7 | Helm chart template (`templates/helm/`) | ✅ |
| 1.8 | Four specs context files: security, code quality, API standards, DB migrations | ✅ |
| 1.9 | `AGENTS.md` — AI agent context and guardrails | ✅ |
| 1.10 | `.github/copilot-instructions.md` — Copilot instructions for the spec-kit itself | ✅ |
| 1.11 | Devcontainer — Node 20, Docker-in-Docker, Copilot extensions, port forwards | ✅ |
| 1.12 | Three-repo model — `web-api-<app>`, `web-<app>`, `db-<app>` as independent repos | ✅ |
| 1.13 | Expense-flow example — full working sample from `ideas/` through all three repos | ✅ |
| 1.14 | `.gitignore` — ignores `repos/` but carves out expense-flow example repos | ✅ |
| 1.15 | `ideas/README.md` and `plans/README.md` orientation docs | ✅ |

---

## Phase 2 — Consistency and Gaps

Tighten up loose ends from Phase 1. No new capabilities — just everything working cleanly.

| # | Item | Status |
|---|---|---|
| 2.1 | `CLAUDE.md` — scope Claude Code to the three commands when used directly in this repo | ✅ |
| 2.2 | `.claude/commands/` — Claude Code slash commands (`/design`, `/plan`, `/create`, `/validate`) as an alternative to the Copilot prompts | ✅ |
| 2.3 | DB repo template (`templates/framework-db-starter-kit/`) — Flyway config, docker-compose, devcontainer, README stub for `db-<app>` | ✅ |
| 2.4 | Update `/validate` prompt to use three-repo paths (`web-api-<app>/`, `web-<app>/`, `db-<app>/`) — currently points to legacy single-repo layout | ✅ |
| 2.5 | Update `AGENTS.md` to reflect three-repo model (currently references `backend/`, `frontend/`, `db/` under one root) | ✅ |
| 2.6 | Update `README.md` to reference `requirements/expense-flow/` as the example (currently says `requirements/example/`) and reflect three-repo local dev setup | ✅ |
| 2.7 | `specs/context/05-frontend.md` — distilled frontend audit rules (hook rules, no inline API calls, `static-config.json` pattern) to match the detail of the other four context files | ✅ |
| 2.8 | `repos/README.md` — update to reflect three-repo model and sibling-clone requirement | ✅ |
| 2.9 | `specs/context/06-framework.md` — deeper context for `FRAMEWORK_SPECS.md` usage: specific `framework-nodejs-fastify` plugin conventions, `framework-react-core` auth flow and hook contracts, `lib-seamlesscomponents-react` theme API, and `framework-eslint-config` rule set — so `/create` and `/validate` can enforce framework-specific patterns rather than just generic TypeScript standards | ✅ |

---

## Phase 3 — Richer Workflow

New commands and improvements that make the tool faster and more reliable to use day-to-day.

| # | Item | Status |
|---|---|---|
| 3.1 | `/iterate <app> <feature>` prompt — adds a new feature to an already-scaffolded app: reads the existing plan, generates a feature branch plan, scaffolds the new routes/service/page | ✅ |
| 3.2 | `/audit-idea <app>` prompt — pre-flight check on `ideas.md` before running `/design`; flags missing sections, vague scope, or missing out-of-scope definition | ✅ |
| 3.3 | Image-first design support — improve `/design` handling of Figma screenshots to extract page layout, navigation, and component names directly into `LLD.md` and `CUSTOMER_PERSONAS.md` | ✅ |
| 3.4 | `ideas/README.md` — add a second `ideas.md` template for API-only apps (no frontend) | ✅ |
| 3.5 | Multi-idea support — allow `ideas/<app>/` to contain multiple `*.md` files merged by `/design` (e.g., `ideas.md` + `technical-constraints.md` + `competitor-notes.md`) | ✅ |
| 3.6 | `/validate` — extend to audit the three-repo docker-compose files (correct Flyway mount, no hardcoded env values, health checks present) | ✅ |

---

## Phase 4 — Additional Stacks

Extend to support a second backend language or framework while keeping the same workflow.

| # | Item | Status |
|---|---|---|
| 4.1 | Python FastAPI backend template (`templates/framework-python-starter-kit/`) — same structural conventions: feature-based routes, service layer, Zod-equivalent (Pydantic), parameterised SQL | ⬜ |
| 4.2 | Stack selector in `/create` — detect from `PLAN.md` or prompt user; route to correct template | ⬜ |
| 4.3 | `specs/context/` additions for Python stack — security, code quality, and API standards translated to Python conventions | ⬜ |
| 4.4 | Second example app using the Python stack (equivalent depth to expense-flow) | ⬜ |

---

## Phase 5 — Automation and Maintenance

Make the tool self-maintaining and integrated with the CI/CD pipeline.

| # | Item | Status |
|---|---|---|
| 5.1 | GitHub Actions workflow — run `/validate` automatically on any PR that touches `repos/` | ⬜ |
| 5.2 | Template version tracking — record which template version was used at `/create` time; surface a warning when templates have been updated since the app was scaffolded | ⬜ |
| 5.3 | Spec drift detection — when a `specs/` file changes, open a checklist issue listing all generated apps that should be reviewed for compliance | ⬜ |
| 5.4 | `ideas/` prompt helper — VS Code snippet or script that creates a correctly-structured `ideas/<app>/ideas.md` stub in one command | ⬜ |
